# sistema_perguntas_node

Pre-requisito ter o docker instalado na maquina.


Para rodar o projeto são necessário 3 comandos.

$ npm install

$ docker-compose up db

$ docker-compose up app

Pronto projeto rodando.
